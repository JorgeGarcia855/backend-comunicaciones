Backend del proyecto de comunicaciones digitales

hacer lo siguiente

crear venv

```
python -m venv .venv
```

activar venv (depende del SO)

instalar requerimientos
``` 
pip install -r requirements.txt
```

crear db

```
python db_build.py
```

correr proyecto
```
python app.py
```